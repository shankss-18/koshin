// Central API configuration
// Change this ONE line to switch between local and production
const API_BASE = import.meta.env.VITE_API_URL || "https://koshin-uqus.onrender.com";

export default API_BASE;
